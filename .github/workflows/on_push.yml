name: push

on:
  push:
    branches-ignore:
      - 'refs/tags/v'

jobs:
  checks:
    uses: openweathermap/deker-actions/.github/workflows/checks.yml@master
    with:
      package-name: deker_shell
      python-version: '3.9'
